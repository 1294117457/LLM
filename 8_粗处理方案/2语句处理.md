是用embedding进行处理麻烦且可用性不全面
我想专门训练一个语句处理llm：具体实现将用户输入语句提纯标准化，不局限于某个业务，而是只针对于提纯对话
但是又有训练样本的问题，如果要专门投喂训练样本又和embedding一样麻烦，所以引入了语句处理llm+高性能大模型适配方案
1.在数据量小时，先通过高性能llm来进行llm提纯作为语句处理llm的数据，
2.当数据量足够时，则开始使用语句处理llm
3.语句处理llm仍可能遇到边缘数据无法处理时，配置回调的功能，让高性能llm来处理边缘数据进行兜底

我这个思路是否可行，同时这样多个llm编排，实际是不是也可以直接使用LangGraph来完成llm的编排，然后我要专注语句处理llm的训练和搭建